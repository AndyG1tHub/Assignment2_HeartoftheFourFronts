package game;

/** Represents the top-level screen or play state of the game. */
public enum GameState {

    INTRO,
    MENU,
    PLAYING,
    PAUSED,
    GAME_OVER_EFFECT,
    WIN_EFFECT,
    GAME_OVER,
    WIN
}